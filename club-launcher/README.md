# Club System Launcher

This project builds a small Windows GUI launcher for the club system.

Build:

```powershell
dotnet publish .\club-launcher\club-launcher.csproj -c Release -r win-x64 --self-contained false
```
